#pragma once

// checks if input string is in the text and returns position of its first element in the text if it is in the text, returns -1 if it is not
int findFirstOccurence(char* fileName, char* string);